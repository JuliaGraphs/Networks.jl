coverage:
  status:
    patch:
      default:
        target: '100'
    project:
      default:
        target: '50'
comment:
  layout: "header, diff"
  behavior: default
  require_changes: false  # if true: only post the comment if coverage changes
  branches: null
  flags: null
  paths: null
